/**
 * Feature Flags
 *
 * Centralized feature toggle configuration.
 * Set to false to disable features without removing code.
 */

/**
 * History Page Feature
 * @archived 2025-10-04
 * @reason Redundant with week/day navigation, simplify UI
 * @reactivate Set to true, re-register route, re-enable nav links, restore tests
 */
export const HISTORY_ENABLED = false;

// Add more feature flags here as needed
// export const FEATURE_X_ENABLED = true;
