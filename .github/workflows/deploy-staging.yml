name: Deploy Staging

on:
  push:
    branches: [develop]

permissions:
  contents: read

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    environment: staging
    env:
      VITE_APP_ENV: staging

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Run linting (optional)
        run: npm run lint --if-present
        continue-on-error: true

      - name: Run type checking (optional)
        run: npm run typecheck --if-present
        continue-on-error: true

      - name: Build application
        run: npm run build

      - name: Copy CNAME for staging
        run: |
          if [ -f "ops/pages/CNAME.staging" ]; then
            cp ops/pages/CNAME.staging dist/CNAME
          fi

      - name: Deploy to staging pages repo
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          repository-name: NewRealm-Projects/winter-arc-app-staging
          branch: gh-pages
          folder: dist
          token: ${{ secrets.PAGES_DEPLOY_TOKEN }}
          clean: true
          commit-message: "Deploy staging build from ${{ github.sha }}"
